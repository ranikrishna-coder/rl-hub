# GitHub Pages Deployment Guide

AgentWork Simulator frontend can be deployed to GitHub Pages, with the API hosted separately.

## 🎯 Architecture

- **Frontend (GitHub Pages)**: Static HTML/CSS/JS - Fast, free CDN
- **Backend API**: FastAPI server - Handles all API requests (deploy to Azure or any cloud platform)

## 🚀 Quick Setup

### Step 1: Enable GitHub Pages

1. Go to your GitHub repository: `ranikrishna-coder/agentwork-simulator`
2. Click **Settings** → **Pages**
3. Under **Source**, select:
   - **Source**: `GitHub Actions`
4. Save

### Step 2: Deploy API Backend

The frontend needs an API endpoint. Deploy the API to your preferred cloud platform (e.g., Azure, Docker on cloud, VPS).

### Step 3: Configure API URL

**Option A: Update Workflow (Recommended)**

Edit `.github/workflows/deploy-github-pages.yml`:

```yaml
- name: Prepare static files
  env:
    API_URL: 'https://your-api-url.com'  # Your API URL
  run: |
    # ... rest of the script
```

**Option B: Update After Deployment**

After GitHub Pages deploys, edit the `config.js` file in the repository or update the workflow.

### Step 4: Push to Deploy

```bash
git add .
git commit -m "Enable GitHub Pages deployment"
git push origin main
```

The workflow will automatically:
1. Build the static frontend
2. Create `config.js` with your API URL
3. Deploy to GitHub Pages

### Step 5: Access Your App

After deployment (2-3 minutes), your app will be at:
- **Frontend**: `https://ranikrishna-coder.github.io/agentwork-simulator/`
- **API**: `https://your-api-url.com` (or your API URL)

## 📋 How It Works

1. **GitHub Actions** runs on every push to `main`
2. **Workflow** copies static files to `_site/` directory
3. **Creates** `config.js` with your API endpoint
4. **Deploys** to GitHub Pages
5. **Frontend** loads `config.js` and connects to your API

## 🔧 Configuration

### API URL Configuration

The frontend uses `config.js` to determine the API endpoint:

```javascript
// config.js (auto-generated by workflow)
window.API_BASE = 'https://your-api-url.com';
```

### Custom API URL

To use a different API URL:

1. **Edit the workflow** (`.github/workflows/deploy-github-pages.yml`):
   ```yaml
   env:
     API_URL: 'https://your-api-url.com'
   ```

2. **Or manually edit** `config.js` after deployment

3. **Or set via environment variable** in GitHub Actions secrets

## 🌐 Custom Domain

You can use a custom domain for GitHub Pages:

1. Go to **Settings** → **Pages**
2. Under **Custom domain**, enter your domain
3. Configure DNS as instructed
4. Your app will be at: `https://yourdomain.com`

## 🔄 Update Process

Every time you push to `main`:
1. GitHub Actions automatically rebuilds
2. Deploys new version to GitHub Pages
3. Takes 1-2 minutes

## 🐛 Troubleshooting

### Frontend Can't Connect to API

1. **Check CORS**: Make sure your API allows requests from GitHub Pages domain
2. **Check API URL**: Verify `config.js` has the correct API URL
3. **Check API Status**: Ensure your API is running and accessible

### CORS Issues

If you see CORS errors, update your API (`api/main.py`):

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://ranikrishna-coder.github.io",
        "http://localhost:8000",
        "https://your-api-url.com"
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### API Not Found

- Verify your API is deployed and running
- Check the API URL in `config.js`
- Test API directly: `curl https://your-api-url.com/`

## 📊 Deployment Status

Check deployment status:
1. Go to **Actions** tab in GitHub
2. Look for "Deploy to GitHub Pages" workflow
3. Click to see logs and status

## 🎨 What Gets Deployed

- ✅ `index.html` - Main catalog page
- ✅ `simulation-console.html` - Simulation interface
- ✅ All CSS files
- ✅ All JavaScript files
- ✅ `config.js` - API configuration (auto-generated)

## 🔐 Security Notes

- GitHub Pages is public - don't put secrets in frontend code
- API keys should be in backend only
- Use HTTPS for all API calls

## 📝 File Structure

```
agentwork-simulator/
├── .github/
│   └── workflows/
│       └── deploy-github-pages.yml  # Deployment workflow
├── api/
│   └── static/                      # Frontend files
│       ├── index.html
│       ├── app.js
│       ├── styles.css
│       └── ...
└── _site/                           # Generated (not in repo)
    ├── index.html
    ├── config.js                    # Auto-generated
    └── ...
```

## 🚀 Next Steps

1. ✅ Enable GitHub Pages in repository settings
2. ✅ Deploy API backend
3. ✅ Update API_URL in workflow
4. ✅ Push to trigger deployment
5. ✅ Access your app at `https://ranikrishna-coder.github.io/agentwork-simulator/`

## 💡 Benefits of This Setup

- ✅ **Free hosting** for frontend (GitHub Pages)
- ✅ **Fast CDN** - GitHub Pages uses global CDN
- ✅ **Automatic HTTPS** - SSL certificate included
- ✅ **Easy updates** - Just push to GitHub
- ✅ **Separate scaling** - Frontend and API scale independently

